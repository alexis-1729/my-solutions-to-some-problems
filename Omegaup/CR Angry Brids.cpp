#include <bits/stdc++.h>

using namespace std;
int n,vx[25];int dp[17];
int solve(int i){
     if(tr>(3*60) || i>=n)return 0;
     ans=dp[i];
     if(ans!=-1)return ans;

     else
         ans=max()


}
main()
{ios_base::sync_with_stdio(0);cin.tie(0);
  cin>>n;
  for(int i=0;i<n;i++)cin>>vx[i];

    memset(dp,-1,sizeof(dp));
  cout<<solve();
}
